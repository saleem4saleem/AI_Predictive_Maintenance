"""Machine learning layer for predictive maintenance.

The frontend must never import this package directly. ML internals are exposed
only through backend services and stable /api/v1 response contracts.
"""
